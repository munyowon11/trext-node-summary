# trext-node-summary

Read-only Slurm node status summary.

## Purpose

`trext-node-summary` is a small utility for summarizing Slurm node and partition status from text output.

The goal is to make Slurm status output easier to review without changing cluster state.

## Scope

This repository focuses on:

- parsing sample `sinfo` output
- summarizing node states
- counting common states such as `idle`, `alloc`, `down`, `drain`, and `mix`
- producing a simple one-page status summary
- keeping all behavior read-only

## Non-goals

This project does not:

- control Slurm
- modify Slurm configuration
- drain, resume, reboot, or modify nodes
- send commands to cluster infrastructure
- connect to external services
- upload or transmit cluster data
- process private operational data
- claim root cause, savings, reliability improvement, or production readiness

## Safety Boundary

This repository is read-only by design.

Any output generated by this tool should be treated as a review aid, not as an operational decision or automated control signal.

Human review is required before taking any operational action.

## Community review

This repository is currently seeking narrow technical feedback from HPC / Slurm operators.

Useful feedback should focus on:

- whether the sample `sinfo` format is realistic enough for a first review;
- whether the node-state summary is understandable;
- which states or fields are missing;
- what the tool should never infer automatically;
- what data should never be requested in a public example.

See [`docs/COMMUNITY_REVIEW_GUIDE.md`](docs/COMMUNITY_REVIEW_GUIDE.md).

Do not share private cluster logs, real node names, account information, credentials, screenshots of internal systems, or production exports.

## Public repository readiness

Before broad external sharing or major public announcement, review:

- [`docs/PUBLIC_REPOSITORY_OPERATOR_GUIDE.md`](docs/PUBLIC_REPOSITORY_OPERATOR_GUIDE.md) — operator behavior and public-release precautions.
- [`docs/PUBLIC_SHARING_CHECKLIST.md`](docs/PUBLIC_SHARING_CHECKLIST.md) — required gate before public sharing.
- [`SECURITY.md`](SECURITY.md) — private reporting route for vulnerabilities or sensitive-data exposure.
- [`CONTRIBUTING.md`](CONTRIBUTING.md) — contribution rules that preserve read-only and no-claim boundaries.

No Human Approval = No Public Release.

## Sample mode

This repository currently provides sample input and sample output only.

This sample-mode release does not connect to a live Slurm cluster.

To review the sample input:

```text
examples/sinfo_sample.txt
```

To review the sample output:

```text
docs/sample-output.md
```

To review the evidence packet sample:

```text
docs/evidence-packet-sample.md
```

## Run sample summary

```bash
bash scripts/trext-node-summary.sh
```

This command reads the sample input file only.
It does not connect to a live Slurm cluster.

## Public safety boundary

This tool is intended to be read-only.

It must not:

- change Slurm configuration
- drain, resume, reboot, or modify nodes
- send control commands
- upload cluster data
- determine root cause automatically
- claim cost savings or operational improvement

Any operational action requires human review.

## Planned Input

Initial input should be sample text similar to:

```text
PARTITION AVAIL  TIMELIMIT  NODES  STATE NODELIST
gpu          up   infinite      2  alloc gpu-[001-002]
gpu          up   infinite      1   idle gpu-003
gpu          up   infinite      1  drain gpu-004
cpu          up   infinite      3   idle cpu-[001-003]
```

## Planned Output

Initial output should be a short summary such as:

```text
Slurm Node Status Summary

Total nodes: 7
Idle: 4
Allocated: 2
Drain: 1
Down: 0
Mixed: 0

Review notes:
- 1 node is marked drain.
- Check drain reason before making any operational decision.
- This report is read-only and does not determine root cause.
```
